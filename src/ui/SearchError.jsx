function SearchError({ children }) {
  return (
    <div className=" h-full  text-center w-full  text-accent flex items-center justify-center text-lg ">
      {children}
    </div>
  );
}

export default SearchError;
